# Obfuscated Arga ys

Open the word document with `macros` disabled off.

See the `macro` by going to `Tools -> Macros -> Visual Basic Editor`.

Run the provided code in `clang` to get the flag.

> Flag : HZ{m@CRO_SH3LL_C000DE}