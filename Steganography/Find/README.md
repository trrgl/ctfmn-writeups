# Find

Only filter the `Base32` alphabet letters from the text and decode it using `Base32`.

We can see that it's `Base32` because it has a `=====` padding with a `5` length which only happens in either `Base32` or `Base96`.

[Solution Code](./sol.py)

> Flag : hz{0c4de3e3f24df1bc67c81e7100918ed3}